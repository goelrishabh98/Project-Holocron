\documentclass[11pt,letterpaper]{article}

\usepackage[margin=0.6in]{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{fontawesome5}

% Colors
\definecolor{accent}{HTML}{38BDF8}
\definecolor{heading}{HTML}{1E293B}

% Section formatting
\titleformat{\section}{\large\bfseries\color{heading}}{}{0em}{}[\titlerule]
\titlespacing*{\section}{0pt}{10pt}{6pt}

% List formatting
\setlist[itemize]{nosep, left=0pt, label=\textbullet, itemsep=2pt}

% No page numbers
\pagestyle{empty}

\begin{document}

% --- Header ---
\begin{center}
  {\LARGE\bfseries \VAR{meta.name|tex}}\\[4pt]
  {\color{accent}\VAR{meta.title|tex}}\\[6pt]
  \href{mailto:\VAR{meta.email|tex}}{\faEnvelope\ \VAR{meta.email|tex}} \enspace$\cdot$\enspace
  \faMapMarker*\ \VAR{meta.location|tex}\\
  \href{https://\VAR{meta.website|tex}}{\faGlobe\ \VAR{meta.website|tex}} \enspace$\cdot$\enspace
  \href{https://\VAR{meta.linkedin|tex}}{\faLinkedin\ LinkedIn} \enspace$\cdot$\enspace
  \href{https://\VAR{meta.github|tex}}{\faGithub\ GitHub}
\end{center}

% --- Education ---
\section{Education}
\BLOCK{for edu in education}
\textbf{\VAR{edu.institution|tex}} \hfill \VAR{edu.date|tex}\\
\VAR{edu.degree|tex}\BLOCK{if edu.gpa} \enspace|\enspace GPA: \VAR{edu.gpa|tex}\BLOCK{endif}
\BLOCK{if edu.highlights}
\begin{itemize}
  \BLOCK{for item in edu.highlights}
  \item \VAR{item|tex}
  \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}
\BLOCK{if not loop.last}\vspace{4pt}\BLOCK{endif}
\BLOCK{endfor}

% --- Experience ---
\section{Experience}
\BLOCK{for job in experience}
\textbf{\VAR{job.company|tex}} \hfill \VAR{job.location|tex}\\
\textit{\VAR{job.role|tex}} \hfill \VAR{job.date|tex}
\begin{itemize}
  \BLOCK{for bullet in job.bullets}
  \item \VAR{bullet|tex}
  \BLOCK{endfor}
\end{itemize}
\BLOCK{if not loop.last}\vspace{4pt}\BLOCK{endif}
\BLOCK{endfor}

% --- Skills ---
\section{Skills}
\BLOCK{for cat in skills.categories}
\textbf{\VAR{cat.name|tex}:} \VAR{cat['items']|join(', ')|tex}\BLOCK{if not loop.last}\\[2pt]\BLOCK{endif}
\BLOCK{endfor}

% --- Selected Projects ---
\BLOCK{if projects}
\section{Selected Projects}
\BLOCK{for proj in projects}
\textbf{\VAR{proj.title|tex}} --- \textit{\VAR{proj.tagline|tex}}\BLOCK{if proj.repo} \enspace\href{\VAR{proj.repo}}{[repo]}\BLOCK{endif}\\
Tags: \VAR{proj.tags|join(', ')|tex}
\BLOCK{if not loop.last}\vspace{4pt}\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endif}

% --- Publications ---
\BLOCK{if publications}
\section{Publications}
\BLOCK{for pub in publications}
\VAR{pub.authors|join(', ')|tex}. ``\VAR{pub.title|tex}.'' \textit{\VAR{pub.venue|tex}}, \VAR{pub.date}.\BLOCK{if pub.doi} \href{https://doi.org/\VAR{pub.doi}}{DOI}\BLOCK{endif}
\BLOCK{if not loop.last}\vspace{4pt}\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endif}

% --- Patents ---
\BLOCK{if patents}
\section{Patents}
\BLOCK{for pat in patents}
\textbf{\VAR{pat.title|tex}} \hfill \VAR{pat.year}\BLOCK{if pat.number} \enspace(\VAR{pat.number|tex})\BLOCK{endif} --- \textit{\VAR{pat.status|tex}}
\BLOCK{if not loop.last}\vspace{4pt}\BLOCK{endif}
\BLOCK{endfor}
\BLOCK{endif}

% --- Awards ---
\BLOCK{if awards}
\section{Awards \& Achievements}
\begin{itemize}
  \BLOCK{for award in awards}
  \item \VAR{award|tex}
  \BLOCK{endfor}
\end{itemize}
\BLOCK{endif}

\end{document}
